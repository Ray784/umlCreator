/**
 * The FileSystemEntityType enum represents the types of entities that can exist in a file system.
 * It can be one of the following:
 * - FILE: Represents a file entity.
 * - DIRECTORY: Represents a directory entity.
 * - UNKNOWN: Represents an unknown entity type.
 */
package com.draw.uml.model.enums;

public enum FileSystemEntityType {
    FILE, DIRECTORY, UNKNOWN;
}
